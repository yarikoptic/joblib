--- a/conftest.py
+++ b/conftest.py
@@ -6,6 +6,8 @@ from _pytest.doctest import DoctestItem
 import logging
 from joblib.parallel import mp
 
+# Would crash on older systems
+collect_ignore = ["joblib/test/_openmp_test_helper/setup.py"]
 
 def pytest_collection_modifyitems(config, items):
     skip_doctests = True
