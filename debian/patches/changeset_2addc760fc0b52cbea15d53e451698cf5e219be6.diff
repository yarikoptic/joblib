From: Jim Crist <jcrist@users.noreply.github.com>
Subject: Pytest warnings plugin causing errors (#525)

--- a/setup.cfg
+++ b/setup.cfg
@@ -16,6 +16,7 @@ universal=1
 addopts =
     --doctest-glob="doc/*.rst"
     --doctest-modules
+     -p no:warnings
 testpaths = joblib
 
 [flake8]
